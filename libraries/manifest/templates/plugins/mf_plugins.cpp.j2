#include <appbase/application.hpp>
#include <boost/preprocessor/seq/for_each.hpp>

#include <dpn/manifest/plugins.hpp>

{% for plugin in plugins %}
#include <dpn/plugins/{{ plugin["plugin_name"] }}/{{ plugin["plugin_name"] }}_plugin.hpp>
{% endfor %}

namespace dpn { namespace plugins {

void register_plugins()
{
   {% for plugin in plugins %}
   appbase::app().register_plugin< dpn::plugins::{{ plugin["plugin_namespace"] }}::{{ plugin["plugin_name" ] }}_plugin >();
   {% endfor %}
}

} }
